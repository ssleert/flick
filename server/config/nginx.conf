worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location / {
            fastcgi_pass backend:9000;
            include fastcgi_params;
        }
    }
}
